import {StackNavigationOptions} from '@react-navigation/stack';

export const rootOptions: StackNavigationOptions = {
  gestureEnabled: true,
  headerShown: false,
};

export const loginOptions: StackNavigationOptions = {
  gestureEnabled: true,
  headerShown: false,
};

export const homeOptions: StackNavigationOptions = {
  //
};
